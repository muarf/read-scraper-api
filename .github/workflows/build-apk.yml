name: Build Android APK

on:
  push:
    branches: [main, master, feature/*]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: mobile/package-lock.json

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Install npm dependencies
        working-directory: ./mobile
        run: npm ci

      - name: Build web assets
        working-directory: ./mobile
        run: |
          mkdir -p www
          cp index.html www/

      - name: Sync Capacitor
        working-directory: ./mobile
        run: npx cap sync android

      - name: Register BnfLogin plugin
        run: |
          PLUGINS_FILE="mobile/android/app/src/main/assets/capacitor.plugins.json"
          if [ -f "$PLUGINS_FILE" ]; then
            python3 -c "
          import json
          with open('$PLUGINS_FILE', 'r') as f:
              plugins = json.load(f)
          if not any(p.get('classpath', '').endswith('BnfLoginPlugin') for p in plugins):
              plugins.append({
                  'pkg': 'BnfLogin',
                  'classpath': 'io.qzz.pressecraper.BnfLoginPlugin'
              })
              with open('$PLUGINS_FILE', 'w') as f:
                  json.dump(plugins, f, indent=2)
              print('BnfLogin plugin registered')
          else:
              print('BnfLogin plugin already registered')
          "
          else
            echo "capacitor.plugins.json not found, creating..."
            echo '[{"pkg":"BnfLogin","classpath":"io.qzz.pressecraper.BnfLoginPlugin"}]' > "$PLUGINS_FILE"
          fi

      - name: Allow cleartext HTTP traffic
        run: |
          MANIFEST="mobile/android/app/src/main/AndroidManifest.xml"
          if [ -f "$MANIFEST" ]; then
            # Add usesCleartextTraffic="true" to <application> tag
            sed -i 's/<application /<application android:usesCleartextTraffic="true" /g' "$MANIFEST"
            echo "Patched AndroidManifest.xml"
            grep "usesCleartextTraffic" "$MANIFEST"
          else
            echo "AndroidManifest.xml not found!"
            find mobile/android -name "AndroidManifest.xml" 2>/dev/null
          fi

      - name: Build APK
        working-directory: ./mobile/android
        run: ./gradlew assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug.apk
          path: mobile/android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30
